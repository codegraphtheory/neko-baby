# {{display_name}}

{{description}}

## Install

```bash
hermes profile install github.com/YOUR_ORG/{{profile_slug}} --alias
{{profile_slug}} chat
```

For local development:

```bash
hermes profile install . --name {{profile_slug}}-dev --alias --yes
{{profile_slug}}-dev chat
```

## Environment

Copy `.env.EXAMPLE` to `.env` and fill in required values.

```bash
cp .env.EXAMPLE .env
```

## Validate

```bash
python3 scripts/validate_profile.py .
```

## Purpose

{{description}}
