# {{display_name}}: Agent Instructions

Read this before editing the profile.

## Mission

Maintain a Hermes profile distribution for: {{description}}

## Rules

1. Never commit `.env`, secrets, memories, sessions, logs, or state databases.
2. Keep `distribution.yaml` valid and installable.
3. Keep `SOUL.md` aligned with the profile mission.
4. Run `python3 scripts/validate_profile.py .` after edits.
5. Update README when install or usage changes.

## Validation

```bash
python3 scripts/validate_profile.py .
```
